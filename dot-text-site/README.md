# ASCII Matrix Field

Static visual test for a black-background ASCII interface. The idle state is a falling character waterfall; typed input is sampled into large text made from the same ASCII grid.

Open `index.html` directly in a browser, or serve this folder locally:

```bash
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.
