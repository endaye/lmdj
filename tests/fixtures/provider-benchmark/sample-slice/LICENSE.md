# CC0 dedication for generated Stage 12 smoke fixtures

Zhang Yuancheng applies the
[CC0 1.0 Universal dedication](https://creativecommons.org/publicdomain/zero/1.0/legalcode)
only to the generated `*.wav` files and generated `manifest.json` located in
this directory.

Those files are original, purely synthetic test material created from fixed
integer parameters by
`tools/provider-benchmark/generate_sample_slice_smoke.py`. They contain no
recording, downloaded audio, third-party sample, model output, or material from
`demos/`.

The generator source, tests, documentation, and every other repository file
remain governed by the repository root `LICENSE`; this CC0 dedication does not
extend to them.
