# Workers

There is no active Worker in the M1 Headless Core Proof.

This directory is reserved for a future out-of-process `provider-host` that
will execute Capability Providers behind the same Provider SDK and Artifact
contracts used in-process.

The retired Patch/Materials audio worker must not be restored here. Remote
execution, retries, timeout, cancellation, and distributed Job/Event lifecycle
require the later Provider Conformance Lab.
