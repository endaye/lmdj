# Historical documentation snapshot storage

The active project moved to [`../docs-site/`](../docs-site/README.md).
This directory retains only the stable snapshot storage namespace, including
version inventories and authenticated provenance. Do not relocate or rewrite
frozen files merely to rename the active site.
