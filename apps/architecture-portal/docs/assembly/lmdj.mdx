---
title: LMDJ Product Assembly
area: assembly
status: implemented
owners: [product, core, release]
source_paths: [products/lmdj, products/lmdj/assembly.lock.json]
---

import {BuildIdentity} from '@site/src/components/BuildIdentity';

<BuildIdentity />

## 当前组合
当前 source 为 Product Build `1.0.35.0` 在实时渲染路径加入恰好 96 帧（固定 48 kHz 下 2 ms）的线性 attack/release 斜坡（F6 修复）：Audio Runtime 升至 `0.5.1`，并按 Core Module 级联把 Application Facade 升至 `1.4.5`、Web Runtime Platform 升至 `0.3.5`、CLI 升至 `1.0.17`、MCP 升至 `1.1.14`、Native Test Host 升至 `1.0.15`、Formal Web Runtime Host 升至 `1.2.14`、Creator Web Host 升至 `1.5.4`；Contract、Provider 与其他 Core Module 身份不变，循环接缝 crossfade 按 2026-08-24 决策暂缓。Product Build `1.0.34.0` / Creator `1.5.3` 曾交付提交前输入电平门（数字静音拒绝）与可见输入身份（F4）；`1.0.33.0` / Creator `1.5.2` 曾交付 `DUPLICATE_ID` 可恢复呈现；`1.0.32.0` / Creator `1.5.1` 曾交付 trim 握把指针模型；`1.0.31.0` / Creator `1.5.0` 曾交付 Capture 面板视口锚定模态化；`1.0.30.0` / Creator `1.4.0` 曾交付 Capture pre-commit Crop；Product Build `1.0.29.0` / Creator `1.3.6` 曾交付修正后的选区波形缩放；`1.0.28.0` / Creator `1.3.5` 是独立审查未通过且从未合并或发布的 branch-local 候选，其不可变快照仅保留为拒绝证据。
Product Build `1.0.25.0` 曾分配 G2 evidence-durability 级联，同样不新增公开产品能力。
Product Build `1.0.24.0` 曾分配八个 Core Module、五个 Host、两个本地 Proof Provider 与八个 active Contract。与前一构建相比，Provider SDK 升至 `1.1.2`，Application Facade 升至 `1.4.1`，Web Runtime Platform 升至 `0.3.1`，CLI/MCP/Native/Creator/Formal Web Runtime Host 与两个本地 Proof Provider 也同步完成 PATCH 级联；Contract 集合与 Provider policy 不变。

Product Build `1.0.22.0` 的 LMDJ Assembly 组合八个 Core Module、五个 Host、两个本地 Proof Provider 与八个 active Contract，其中新增 `lmdj.project.v2 2.0.0`。它保留 Project I/O Web publication/lease recovery、Browser Main fail-closed、Creator recovery observability、共享 keyboard mapping、MIDI channels 1–16 与 generation cleanup 约束，并加入完整 Sample Editor。Authoring Domain `0.2.0`、Project I/O `0.6.0`、Project Cooker `0.3.0`、Audio Runtime `0.5.0`、Application Facade `1.4.0`、Web Runtime Platform `0.3.0`、CLI `1.0.12`、MCP `1.1.9`、Native Test Host `1.0.10`、Web Runtime Host `1.2.9` 与 Creator Web Host `1.2.0` 是本构建锁定的精确身份；Foundation、Provider SDK、Provider、Capability 与 model identity 保持不变。
## 编译接线
`products/lmdj/src/compiled_assembly.cpp` 是产品专属 wiring；产品中立模块不知道 LMDJ 的具体组合。
Product CMake 只在 Emscripten `lmdj_web_runtime_host` target 存在时链接 compiled catalog object，并注入 Product Build、Platform version 以及 Creator/Diagnostic 两组精确 distribution-contract/Host identity。两个 Web Host 只声明 Web Runtime Platform 依赖；Platform 声明 Facade/Audio 依赖，Provider wiring 仍只属于 Product Assembly。
## Policy
当前 Provider policy 仅允许 local/public/proof.execute 范围，与 Proof-only Provider 相符。
## 身份与完整性
Product Build、每个组件版本和 source hash 被锁定；门户事实生成会拒绝任何漂移。
Project Bundle Contract 仍是 portable transfer 边界；Project v2 是新的创作真相 Contract；同构建 Web transport 仍不是公开 Contract。Provider、Capability 与 model identity 未变化。
## 限制
Sequence/Take、持久化 Asset 编辑、Crop Undo、生产 Provider、云能力、物理 Web 验收与 `beta`/`stable` 均不能由当前 Assembly 成员关系或门户快照推导。Build Identity 只证明当前页面绑定的 Product/Assembly 身份。
