---
title: Version 与 Release
area: operations
status: implemented
owners: [release, core]
source_paths: [docs/governance/version-management.md, docs/governance/git-workflow.md, scripts/version.py]
---

import {BuildIdentity} from '@site/src/components/BuildIdentity';

<BuildIdentity />

## 四条版本轴
Product Build 使用四段式；Core Module、Host 与 Provider 使用独立 SemVer；Contract 使用稳定 ID + Contract SemVer。
## Assembly Lock
锁文件记录确切组件版本与 source hash，门户同样从它生成身份，防止文档手填漂移。
当前候选 Product Build 是 `1.0.14.0 · canary · g<short-sha>`；Web Runtime Host `1.0.0` 与所有受影响 Module/Host identity 已进入 Assembly 和生成 lock。Contracts、Providers 与 Models 未变化。
## Git 与发布
Task commit、push、PR、squash merge、signed product tag、Release、部署和 Channel promotion 是不同授权与证据。
## 不可变性
已发布 tag 不移动；交付测试或正式发布的文档快照按 Product Build 冻结，current 文档持续跟随 main。普通 PR Preview 不创建永久快照。
Task 12A 只更新 current truth；由于快照命令要求干净 worktree，`1.0.14.0` canary immutable snapshot 由 Task 12B 在 Task 12A commit 后生成。当前缺少该快照是 Task 12A Portal check 唯一获准的预期例外，不得通过弱化检查消除。
## 发布门禁
实现计划必须声明 Version impact；测试发布和 stable release 前都验证匹配的说明书快照，再分别运行该 Channel 所需的版本验证、测试、CI、产品验收与发布证据门禁。

未来 Product tag 文本为 `lmdj-v1.0.14.0`，但只有 squash merge、全量 CI、merged-main Proof 与独立授权后才能创建 signed annotated tag。本 Task 没有 push、PR、merge、tag、Release、部署、发布或 Channel promotion。
