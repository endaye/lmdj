# Real Portal candidate journey rehearsal

Delivery base: df4e49be9ce7168f7ae25f1e0f1f35fb4b75f032.
Original Task: c259fd12f7fbb1edae4f364f361f75e14a4f922a (base
0fcffb83703fe6d0d0335ddcab9fd407e4e399e0). Original-stack runs below are
historical evidence, not validation of this delivery base. Canonical snapshot,
cut and resume-alias repairs already belong to the delivery parent; this Task
does not reapply or weaken them.

## Task and files

Replace the remaining candidate-path command fixtures in a separate explicit
integration rehearsal with the actual official Portal generator/checker and
cut controller. Keep the normal fast/contract suites unchanged.

Declared files:

- tests/build/release_candidate_portal_journey.py
- apps/docs-site/docs/operations/version-and-release.mdx
- docs/plans/2026-09-13-release-candidate-portal-journey.md

The harness accepts a fresh evidence directory and explicit trusted tool PATH.
It creates independent local fixture repositories, a fixture-only reservation,
actual source installation, official version/resume commands, a cut checked
before and after commit, a non-shared fresh clone, local squash, official witness
generation in the retained-source repository, witness-byte transfer, and a
final full Portal check in the clone. Assert that the far-side clone lacks
the private source object both before and after verification. Preserve every
repository, journal, command log and exit code; do not overwrite prior runs.
No GitHub mutation, tag, signing, publication, deployment or channel promotion.

Use private diagnostic files to retain the concrete command runner's output;
the test-only backing-file substitution does not replace the executed program,
environment, lock descriptor, timeout or actual exit/digest checks. Fixture
authority checks are not real authenticated intake or branch protection.

## Verification

Run directly, not from npm test or CTest: the journey itself invokes the full
Portal suite repeatedly, and registering it inside those commands would cause
recursive execution and add an unrelated universal PR gate. The existing
registered candidate contract tests remain the lowest-tier regression coverage.

```sh
python3 tests/build/release_candidate_portal_journey.py \
  --evidence-root /absolute/new-private-evidence-directory \
  --tool-path /absolute/node22/bin:/usr/bin:/bin:/absolute/python/bin
```

Install this control worktree's locked Portal dependencies before the run.
Each fixture receives an independent directory copy, not a symlink or hardlinks
that could expose another workspace's dependency files to writes. The harness
records the base commit and its own source digest. Run staged ownership and the
current Portal check before committing this Task.

### Delivery evidence

Current rehearsal root: /tmp/lmdj-candidate-portal-delivery.4trE0h/run-v1.
Outer transcript: /tmp/lmdj-candidate-portal-delivery-journey-v1.log.
The cloned baseline is the delivery parent above. The uncommitted control-side
harness is separately authenticated by its recorded SHA256; snapshot/cut
controller digests are recorded too. Current Portal edits are checked separately,
not silently included in an already enrolled source or immutable snapshot.

The far-side boundaries are: actual installed source receipt; official version
and verification-only resume with successful postchecks; staged and committed
cut checks; independent clone with source object absent; actual local squash;
official witness production in the retained-source repository; transfer of the
exact witness bytes; full Portal check in the clone; final clean checkout and
source object still absent. Each command retains its actual exit and separate
output identity. Fixture authorization and local squash do not stand in for
authenticated intake, actual protected GitHub review/merge or full release CI.

Locked dependencies installed successfully. Staged ownership passed 74/74,
exit 0, 5.883 seconds (/tmp/lmdj-candidate-portal-delivery-scope-v1.log).
The optimized-Python invocation intentionally exited 1 before argument parsing
with the required assertion-enforcement refusal; raw output is retained in
/tmp/lmdj-candidate-portal-delivery-optimized-refusal-v1.log. This expected
negative is not a successful journey.

Delivery run-v1 completed with process exit 0. Actual generation and resume,
three staged and three committed gates, local squash, official witness and the
fresh-clone Portal check all passed. Final clone status returned exit 0 with
empty stdout; its separate stderr retained a 311-byte Git hint. Source-object
absence returned exactly exit 1 both before and after the final verification.
The final clone Portal suite passed 159/159 tests, zero skipped, and 47 routes.

- Source: dc2b88d89e51a5a965e28a2239f419574d08ab93.
- Cut: dcc6e0b1722fda387eb177b96fd4dacab03846e6.
- Fixture squash: e1bf382fa1e71e3b85d40443eb49c6c98178fb3b.
- Snapshot SHA256: 8ea8f770f452bef1fbbd258ad30bc9d523b3eda02810e49be0c97dadef09d28b.
- Witness: 10995 bytes, SHA256
  3141d885e6c9fb93e25bee2185468bc9363cd4d51749e4438c02157290fa6b40.

An outer read-only audit verified all 20 command exits and separate stdout/stderr
lengths/digests, all eight actual executor outputs against the durable snapshot
journal and six cut gate records, the harness/controller file digests, and the
same witness bytes in producer and consumer. Exactly one version and one resume
command were present, with v2 verified count 2. Evidence:
/tmp/lmdj-candidate-portal-delivery-evidence-v1.log. The assertion-audit exit was
0; this validates retained bytes, not a reconstructed command transcript.

This remains a temporary fixture Build only. No release was allocated in the
control repository; no remote mutation, real review/squash, candidate complete16
CI, signing, publication, deployment, promotion or service restart occurred.
The independent review inspected the complete three-file diff and found no
actionable finding; it did not rerun this expensive journey independently.

The control worktree's Node 22 scripts/docs-site.sh check separately exited 0,
with 159/159 tests, zero skipped and 47 routes/internal links:
/tmp/lmdj-candidate-portal-delivery-docs-v1.log. Four assertions against the
generated operations HTML confirm the rehearsal entrypoint and non-acceptance
boundaries are actually rendered:
/tmp/lmdj-candidate-portal-delivery-rendered-v1.log (exit 0).
No production controller was changed in this Task. No new process-only pitfall
was discovered; the retained witness and independent-clone procedure follows
the existing squash-witness-provenance skill obligation.

### Historical original-stack evidence

The following attempts and upstream PR observation describe the original
stack at that time, not current delivery or current GitHub state.

### Retained first attempt

The first attempt stopped before source installation because a node_modules
symlink is not covered by the repository's directory-only ignore rule. The
unrelated-file guard was correct. Fix only the harness to use an independent
real dependency directory; do not edit ignore rules or weaken the guard.
Evidence: /tmp/lmdj-candidate-portal-rQWbMG/run-v1 and
/tmp/lmdj-candidate-portal-journey-v1.log. Subsequent attempts use new directories.

### Retained second attempt and causal repair

The real official generator completed successfully in run-v2, including 47
Portal routes, but the controller rejected its post-command tracked bytes.
The canonical generated files live under apps/architecture-portal; docs-site
exposes tracked compatibility symlinks. The controller and lightweight fixtures
incorrectly used the compatibility paths as Git paths. Align inventory, the
single mutable versions file, cut allowlist and witness staging with canonical
storage. Preserve raw symlink and all other source-byte checks. Retain run-v2
and its actual executed-z1buaudm.log; do not regenerate that enrolled candidate.

Run-v3 passed generation and controller post-command source verification, then
the official resume command tried to parse the committed compatibility symlink
as JSON. Its raw failure remains in logs/executed-ih0w28ok.log. The separate
snapshot-resume-canonical-alias Task repairs that reader; the next fresh
rehearsal must clone its committed fix, not modify an enrolled source.

Canonical-path controller verification: snapshot 14/14 (67.332 seconds) and
cut 11/11 (51.310 seconds), zero failures, in
/tmp/lmdj-candidate-path-snapshot-v3.log and /tmp/lmdj-candidate-path-cut.log.
The lower-tier fixture now commits canonical versions and compatibility links,
writes through the links and asserts canonical inventory. Its first revision
correctly failed because Git omitted an empty symlink-target directory; that
log remains /tmp/lmdj-candidate-path-snapshot-v2.log. A tracked placeholder
repairs the fixture; production checks are unchanged. Independent review clean.

The harness rejects optimized Python (assertions required) and records both
controller file digests alongside its own digest and the cloned source HEAD.
Run-v4 proved generation, resume and both cut verification phases, but the
harness incorrectly attempted witness production in the source-absent clone.
The official producer correctly refused; log retained at
/tmp/lmdj-candidate-portal-rQWbMG/run-v4/logs/014.log. Correct the harness order:
fetch only the introducing fixture commit into the retained-source repository,
generate the official witness there, and copy only its recorded bytes to the
source-absent clone for verification. Do not hydrate the consumer or relax any
producer requirement. Run-v5 passed witness production and the final Portal
check, but the harness merged Git stderr hints into its machine-readable status
output and incorrectly called a clean worktree dirty. Preserve that failed run;
separate stdout and stderr into distinct fsynced, hashed logs rather than
suppressing diagnostics. A read-only follow-up confirmed clean status and the
source object still absent. Run-v6 re-executes the entire corrected harness.
### Completed actual journey

Run-v6 completed with process exit 0. Source generation and resume, all three
staged checks, all three committed checks, official witness generation and
the fresh-clone Portal check passed. Final status stdout is empty (exit 0);
the separately retained Git hint is 311 stderr bytes. The source-object
absence checks both returned the expected exit 1. No command failure was
reclassified as success. The final Portal suite passed 152 tests, zero skipped,
and 47 routes. Raw commands, separate stdout/stderr and controller receipts:
/tmp/lmdj-candidate-portal-rQWbMG/run-v6/events.jsonl and logs/;
outer transcript: /tmp/lmdj-candidate-portal-journey-v6.log.

- Cloned baseline: fc447d4962e5153303188368bf7543ee2f572acc.
- Source: 2e07f987d2bf7e7cbe9a5c8dda19977cbfbb7b1c.
- Cut: dd4c679112b7859d69452fd014fda3891f313f36.
- Fixture squash: 873fd5710f8319a970c57f2e19c4933a0a6dc917.
- Snapshot SHA256: f55591501c58bf340982c71b35f057cce872df0fdec74c5fc5fa1873dab123ef.

This is local fixture Build 1.0.57.0 only, not an allocated live release. The
control-side Python changes are bound by their recorded file digests; the
cloned baseline binds the official Node/shell generator. Current Portal page
changes were separately verified by /tmp/lmdj-resume-alias-portal.log (152 tests,
47 routes) and inspected in the actual built version-and-release HTML.
Independent review covered the path repair, realistic fixture, witness
handoff and split-stream logging; clean after recorded corrections.

Remote candidate PR/review/protected squash, exact-main complete16 CI, service
restart, signing/publication/deployment/promotion and the whole release journey
remain unexercised. This Task does not claim those gates. Upstream #1266 remains
open at c8fab3b67b6abe3bff708b5563acfc84b0e9e671; fresh paginated comments showed
no owner-review attestation, so this stack remains local/unpushed.

Pitfall disposition: these concrete path defects are derivable from the code
and covered by causal regressions; no separate process-ledger entry.

## Version Management

Version impact: none

Reason: explicit temporary-repository rehearsal; no real Product Build or team
testing identity is allocated and no release tag or public artifact is created.

## Documentation Impact

Documentation impact: required

Affected portal pages: /operations/version-and-release/
