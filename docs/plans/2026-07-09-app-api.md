# App Backend (apps/api) v1 Implementation Plan

> **For agentic workers:** Follow repository `AGENTS.md` and execute the approved plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `apps/api/` 落地 `lmdj-api`：把 audio worker 的 `process_job` 包成 FastAPI 后端——上传音频 → 后台线程建 job → 轮询 status → 拉取 patch 产物。

**Architecture:** 薄层（spec 方案 A）：`executor.py`（后台单槽执行器，`threading.Lock` 串行 `process_job`）+ `app.py`（`create_app` 工厂 + 路由，依赖注入 runner/jobs_root）。复用 `lmdj-audio-worker` 的 `process_job`/`read_status` 与文件系统 job 库。设计依据：`docs/design/2026-07-09-lmdj-app-api-design.md`。

**Tech Stack:** Python 3.11+、FastAPI、uvicorn、`lmdj-audio-worker`（本地 editable，间接带入 patchify/core-models）、pytest + FastAPI TestClient（`httpx`）。**不进 torch/numpy<2**。

## Global Constraints

- apps/api 包保持轻量：pyproject runtime deps 仅 `fastapi` + `uvicorn[standard]` + `python-multipart`（上传）；`lmdj-audio-worker`/`lmdj-patchify`/`lmdj-core-models` 走本地 editable 安装（path 依赖不写 metadata，与既有包同惯例）；venv 内不得出现 torch/numpy<2。
- 执行模型：后台 daemon 线程跑 `process_job`；`JobExecutor` 实例级 `threading.Lock` 串行化（任意时刻只有一个 job 真跑）；`process_job` 自身已把异常落盘 failed，executor 线程只兜底 `logging.exception`，不吞不崩。
- 端点：`GET /health`、`POST /uploads`（multipart `file`）、`GET /jobs/{job_id}`、`GET /jobs/{job_id}/patch`、`GET /jobs/{job_id}/files/{path:path}`。
- job_id = `uuid4().hex[:12]`；上传字节先落临时文件，再交 executor（`process_job` 负责把它拷进 `job/input/`）。
- 产物端点服务包内相对路径（`samples/xxx.wav`、`loop_preview.wav`）；路径穿越（绝对路径 / 含 `..` / 逃出包目录）→ 400；`Content-Type` 按扩展名（.wav→audio/wav，.json→application/json，其余 application/octet-stream）。
- 错误：未知 job_id → 404；job 未 completed 取 patch/file → 409（body 含当前 state）；文件缺失 → 404；缺 file 字段 → 422（FastAPI 默认）。
- CORS：dev 放行 `http://localhost:5173`。
- 默认 `jobs_root = apps/api/jobs/`（相对包定位；根 `.gitignore` 增加 `apps/api/jobs/`）；`create_app(runner=None, jobs_root=None)` 工厂缺省 `DemoPipelineRunner(默认 demo-dir)`，测试注入 FakeRunner + tmp jobs_root。
- 测试零 demucs/torch/ffmpeg：apps/api 自己的 conftest 复刻 `FakeRunner`（拷 golden fixture 为 package）+ golden 音频 fixture（不跨包 import worker 的 conftest）。
- patch_id / patch.json 内容契约不由本层定义——直接透传 patchify 产物。
- Commit scope：`feat(api): ...`（Conventional Commits）。
- 范围外：队列、Postgres/对象存储、鉴权、ideas/generation、remix/share/lineage、cancel、Web 侧接入、部署。

---

## File Structure

- Create: `apps/api/pyproject.toml`
- Create: `apps/api/lmdj_api/__init__.py`
- Create: `apps/api/lmdj_api/executor.py`（JobExecutor）
- Create: `apps/api/lmdj_api/app.py`（create_app + 路由 + 模块级 app）
- Create: `apps/api/tests/__init__.py`、`apps/api/tests/conftest.py`（FakeRunner、golden 音频 fixture）
- Create: `apps/api/tests/test_executor.py`
- Create: `apps/api/tests/test_app.py`
- Modify: 根 `.gitignore`（`apps/api/jobs/`）、`apps/README.md`（本地验证命令）

---

### Task 1: Package Skeleton And JobExecutor

**Files:**
- Create: `apps/api/pyproject.toml`
- Create: `apps/api/lmdj_api/__init__.py`
- Create: `apps/api/lmdj_api/executor.py`
- Create: `apps/api/tests/__init__.py`
- Create: `apps/api/tests/conftest.py`
- Create: `apps/api/tests/test_executor.py`
- Modify: 根 `.gitignore`

**Interfaces:**
- Consumes: `lmdj_audio_worker.process_job`（`process_job(audio, *, jobs_root, runner, job_id=None, on_state=None) -> JobStatus`）、`lmdj_audio_worker.PipelineRunner`。
- Produces:
  - `class JobExecutor(runner: PipelineRunner, jobs_root: Path)`：`submit(audio_path: Path, job_id: str) -> None`（起 daemon 线程串行跑 process_job）、`wait_idle(timeout: float = 30) -> None`（测试辅助：阻塞至所有已提交线程结束）。
  - conftest：`FakeRunner`（拷 golden fixture 为 package，可注入 barrier/fail）、`golden_audio` fixture、`GOLDEN` 路径常量。

- [ ] **Step 1: 写 conftest 与 failing 执行器测试**

Create `apps/api/tests/conftest.py`:

```python
from __future__ import annotations

import shutil
import threading
from pathlib import Path

import pytest

# patchify 的 golden fixture（真实 pipeline 输出快照）
GOLDEN = Path(__file__).resolve().parents[3] / "packages" / "patchify" / "tests" / "fixtures" / "testsong"


class FakeRunner:
    """拷 golden fixture 为 package，模拟 pipeline 成功；可注入运行屏障或失败。"""

    def __init__(self, package_source: Path = GOLDEN, barrier: threading.Event | None = None,
                 started: threading.Event | None = None, fail_with: Exception | None = None) -> None:
        self.package_source = package_source
        self.barrier = barrier
        self.started = started
        self.fail_with = fail_with

    def run(self, audio: Path, out_dir: Path, song_id: str) -> Path:
        if self.started:
            self.started.set()
        if self.barrier:
            self.barrier.wait(timeout=5)
        if self.fail_with:
            raise self.fail_with
        dst = out_dir / song_id
        shutil.copytree(self.package_source, dst)
        return dst


@pytest.fixture
def golden_audio(tmp_path: Path) -> Path:
    audio = tmp_path / "song.wav"
    audio.write_bytes(b"RIFF....WAVEfmt fake-audio")
    return audio
```

Create `apps/api/tests/test_executor.py`:

```python
import threading
from pathlib import Path

from lmdj_api.executor import JobExecutor
from lmdj_audio_worker.status import read_status

from tests.conftest import FakeRunner


def test_submit_runs_process_job_to_completed(tmp_path: Path, golden_audio: Path):
    jobs_root = tmp_path / "jobs"
    executor = JobExecutor(runner=FakeRunner(), jobs_root=jobs_root)

    executor.submit(golden_audio, "jobA")
    executor.wait_idle()

    status = read_status(jobs_root / "jobA")
    assert status.state == "completed"
    assert status.patch_id and status.patch_id.startswith("jobA-")


def test_jobs_run_serially_not_concurrently(tmp_path: Path, golden_audio: Path):
    jobs_root = tmp_path / "jobs"
    barrier = threading.Event()
    started = threading.Event()
    # 第一个 job 卡在 barrier；若并发，第二个也会 start
    slow = FakeRunner(barrier=barrier, started=started)
    executor = JobExecutor(runner=slow, jobs_root=jobs_root)

    executor.submit(golden_audio, "jobA")
    assert started.wait(timeout=5)  # jobA 进入 runner
    started.clear()
    executor.submit(golden_audio, "jobB")
    # jobA 仍卡着 → jobB 不应 start（锁串行）
    assert not started.wait(timeout=0.5)

    barrier.set()  # 放行 jobA
    executor.wait_idle()
    assert read_status(jobs_root / "jobA").state == "completed"
    assert read_status(jobs_root / "jobB").state == "completed"


def test_runner_failure_persists_failed_not_crash(tmp_path: Path, golden_audio: Path):
    from lmdj_audio_worker.runner import PipelineRunError

    jobs_root = tmp_path / "jobs"
    executor = JobExecutor(runner=FakeRunner(fail_with=PipelineRunError("boom", stderr_tail="x")),
                           jobs_root=jobs_root)
    executor.submit(golden_audio, "jobA")
    executor.wait_idle()

    assert read_status(jobs_root / "jobA").state == "failed"
```

- [ ] **Step 2: 建包并跑测试确认失败**

Create `apps/api/pyproject.toml`:

```toml
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lmdj-api"
version = "0.1.0"
description = "LMDJ app backend: upload -> job -> status -> patch"
requires-python = ">=3.11"
# 本地依赖（path 依赖不写 metadata）：
#   pip install -e ../../packages/core-models -e ../../packages/patchify -e ../../workers/audio
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "python-multipart>=0.0.9,<1",
]

[project.optional-dependencies]
test = [
  "pytest>=8,<9",
  "httpx>=0.27,<1",
  "jsonschema>=4,<5",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["lmdj_api*"]
```

Create `apps/api/lmdj_api/__init__.py`（空文件，Task 2 补导出）。
Create `apps/api/tests/__init__.py`（空文件，使 `from tests.conftest import ...` 可用，与其他包同解法）。

Modify 根 `.gitignore` 的 `### LMDJ` 节追加:

```gitignore
# App API 本地 job 产物
apps/api/jobs/
```

```bash
cd /Users/endaye/Projects/lmdj/apps/api
python3 -m venv .venv
.venv/bin/pip install -e ../../packages/core-models -e ../../packages/patchify -e ../../workers/audio
.venv/bin/pip install -e ".[test]"
.venv/bin/python -m pytest tests/test_executor.py -q
```

Expected: FAIL——`ModuleNotFoundError: No module named 'lmdj_api.executor'`。

- [ ] **Step 3: 实现 executor.py**

Create `apps/api/lmdj_api/executor.py`:

```python
from __future__ import annotations

import logging
import threading
from pathlib import Path

from lmdj_audio_worker import PipelineRunner, process_job

log = logging.getLogger(__name__)


class JobExecutor:
    """后台单槽执行器：daemon 线程跑 process_job，一个 Lock 串行化。

    demucs 现在走子进程，串行不是线程安全需要，而是避免并发子进程抢爆 CPU/内存。
    """

    def __init__(self, runner: PipelineRunner, jobs_root: Path) -> None:
        self.runner = runner
        self.jobs_root = jobs_root
        self._lock = threading.Lock()
        self._threads: list[threading.Thread] = []

    def submit(self, audio_path: Path, job_id: str) -> None:
        thread = threading.Thread(target=self._run, args=(audio_path, job_id), daemon=True)
        self._threads.append(thread)
        thread.start()

    def _run(self, audio_path: Path, job_id: str) -> None:
        with self._lock:
            try:
                process_job(audio_path, jobs_root=self.jobs_root, runner=self.runner, job_id=job_id)
            except Exception:  # noqa: BLE001 — process_job 已落盘 failed；此处仅兜底不崩线程
                log.exception("job %s crashed outside process_job", job_id)

    def wait_idle(self, timeout: float = 30) -> None:
        """测试辅助：阻塞至所有已提交线程结束。"""
        for thread in list(self._threads):
            thread.join(timeout=timeout)
```

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_executor.py -q
```

Expected: `3 passed`。

- [ ] **Step 5: Commit**

```bash
cd /Users/endaye/Projects/lmdj
git add apps/api .gitignore
git commit -m "feat(api): add background job executor with serializing lock"
```

---

### Task 2: FastAPI App And Routes

**Files:**
- Modify: `apps/api/lmdj_api/__init__.py`
- Create: `apps/api/lmdj_api/app.py`
- Create: `apps/api/tests/test_app.py`
- Modify: `apps/README.md`

**Interfaces:**
- Consumes: `JobExecutor` from Task 1；`lmdj_audio_worker.read_status`（`read_status(job_dir) -> JobStatus`，`JobStatus.to_dict()`）；`lmdj_audio_worker.DemoPipelineRunner`；conftest `FakeRunner`。
- Produces:
  - `create_app(runner: PipelineRunner | None = None, jobs_root: Path | None = None) -> FastAPI`
  - 模块级 `app = create_app()`（供 `uvicorn lmdj_api.app:app`）
  - `lmdj_api` 包导出 `JobExecutor`、`create_app`

- [ ] **Step 1: 写 failing 路由测试**

Create `apps/api/tests/test_app.py`:

```python
import io
import threading
import time
from pathlib import Path

import jsonschema
from fastapi.testclient import TestClient
from lmdj_core_models.model import load_patch_schema

from lmdj_api.app import create_app

from tests.conftest import FakeRunner


def make_client(tmp_path: Path, runner=None) -> TestClient:
    app = create_app(runner=runner or FakeRunner(), jobs_root=tmp_path / "jobs")
    return TestClient(app)


def _upload(client: TestClient) -> str:
    resp = client.post("/uploads", files={"file": ("song.wav", io.BytesIO(b"RIFFfake"), "audio/wav")})
    assert resp.status_code == 200, resp.text
    body = resp.json()
    assert body["state"] == "queued"
    return body["job_id"]


def _poll_completed(client: TestClient, job_id: str, tries: int = 50) -> dict:
    for _ in range(tries):
        body = client.get(f"/jobs/{job_id}").json()
        if body["state"] in ("completed", "failed"):
            return body
        time.sleep(0.05)
    raise AssertionError("job did not finish in time")


def test_health(tmp_path: Path):
    assert make_client(tmp_path).get("/health").json() == {"ok": True}


def test_upload_then_poll_to_completed(tmp_path: Path):
    client = make_client(tmp_path)
    job_id = _upload(client)
    final = _poll_completed(client, job_id)
    assert final["state"] == "completed"
    assert final["patch_id"].startswith(f"{job_id}-")


def test_get_patch_returns_schema_valid_json(tmp_path: Path):
    client = make_client(tmp_path)
    job_id = _upload(client)
    _poll_completed(client, job_id)

    resp = client.get(f"/jobs/{job_id}/patch")
    assert resp.status_code == 200
    data = resp.json()
    jsonschema.validate(data, load_patch_schema())
    assert data["schema"] == "lmdj.patch.v1"


def test_get_sample_file_returns_bytes(tmp_path: Path):
    client = make_client(tmp_path)
    job_id = _upload(client)
    _poll_completed(client, job_id)
    patch = client.get(f"/jobs/{job_id}/patch").json()
    rel = patch["elements"][0]["source_path"]  # e.g. samples/drum_low.wav

    resp = client.get(f"/jobs/{job_id}/files/{rel}")
    assert resp.status_code == 200
    assert resp.headers["content-type"].startswith("audio/")
    assert len(resp.content) > 0


def test_unknown_job_is_404(tmp_path: Path):
    client = make_client(tmp_path)
    assert client.get("/jobs/ghost").status_code == 404
    assert client.get("/jobs/ghost/patch").status_code == 404
    assert client.get("/jobs/ghost/files/samples/x.wav").status_code == 404


def test_patch_before_completed_is_409(tmp_path: Path):
    barrier = threading.Event()
    client = make_client(tmp_path, runner=FakeRunner(barrier=barrier))
    job_id = _upload(client)
    # job 卡在 runner barrier → 尚未 completed
    for _ in range(50):
        if client.get(f"/jobs/{job_id}").json()["state"] in ("separating", "queued", "patchifying"):
            break
        time.sleep(0.02)
    resp = client.get(f"/jobs/{job_id}/patch")
    assert resp.status_code == 409
    assert "state" in resp.json()
    barrier.set()


def test_path_traversal_is_400(tmp_path: Path):
    client = make_client(tmp_path)
    job_id = _upload(client)
    _poll_completed(client, job_id)
    for bad in ["../../../etc/passwd", "/etc/passwd", "samples/../../secret"]:
        resp = client.get(f"/jobs/{job_id}/files/{bad}")
        assert resp.status_code == 400, f"{bad} → {resp.status_code}"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/endaye/Projects/lmdj/apps/api
.venv/bin/python -m pytest tests/test_app.py -q
```

Expected: FAIL——`ModuleNotFoundError: No module named 'lmdj_api.app'`。

- [ ] **Step 3: 实现 app.py**

Create `apps/api/lmdj_api/app.py`:

```python
from __future__ import annotations

import shutil
import tempfile
import uuid
from pathlib import Path

from fastapi import FastAPI, File, HTTPException, UploadFile
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import FileResponse, JSONResponse

from lmdj_audio_worker import DemoPipelineRunner, PipelineRunner
from lmdj_audio_worker.status import read_status

from lmdj_api.executor import JobExecutor

_API_ROOT = Path(__file__).resolve().parent.parent
DEFAULT_JOBS_ROOT = _API_ROOT / "jobs"
DEFAULT_DEMO_DIR = _API_ROOT.parent.parent / "references" / "demos" / "lmdj-song-pipeline"

_CONTENT_TYPES = {".wav": "audio/wav", ".json": "application/json", ".mid": "audio/midi"}


def create_app(runner: PipelineRunner | None = None, jobs_root: Path | None = None) -> FastAPI:
    jobs_root = jobs_root or DEFAULT_JOBS_ROOT
    runner = runner or DemoPipelineRunner(DEFAULT_DEMO_DIR)
    executor = JobExecutor(runner=runner, jobs_root=jobs_root)

    app = FastAPI(title="LMDJ API")
    app.add_middleware(
        CORSMiddleware,
        allow_origins=["http://localhost:5173"],
        allow_methods=["*"],
        allow_headers=["*"],
    )

    def _job_dir(job_id: str) -> Path:
        job_dir = jobs_root / job_id
        if not (job_dir / "status.json").exists():
            raise HTTPException(status_code=404, detail="unknown job_id")
        return job_dir

    @app.get("/health")
    def health() -> dict:
        return {"ok": True}

    @app.post("/uploads")
    async def uploads(file: UploadFile = File(...)) -> dict:
        job_id = uuid.uuid4().hex[:12]
        suffix = Path(file.filename or "input.wav").suffix or ".wav"
        tmp = Path(tempfile.mkdtemp()) / f"upload{suffix}"
        with tmp.open("wb") as out:
            shutil.copyfileobj(file.file, out)
        executor.submit(tmp, job_id)
        return {"job_id": job_id, "state": "queued"}

    @app.get("/jobs/{job_id}")
    def job_status(job_id: str) -> dict:
        return read_status(_job_dir(job_id)).to_dict()

    @app.get("/jobs/{job_id}/patch")
    def job_patch(job_id: str):
        job_dir = _job_dir(job_id)
        status = read_status(job_dir)
        if status.state != "completed":
            return JSONResponse(
                status_code=409,
                content={"detail": "job not completed", "state": status.state},
            )
        patch_path = job_dir / (status.package_dir or "") / "patch.json"
        if not patch_path.exists():
            raise HTTPException(status_code=404, detail="patch.json not found")
        return FileResponse(patch_path, media_type="application/json")

    @app.get("/jobs/{job_id}/files/{path:path}")
    def job_file(job_id: str, path: str):
        job_dir = _job_dir(job_id)
        status = read_status(job_dir)
        if status.state != "completed":
            return JSONResponse(
                status_code=409,
                content={"detail": "job not completed", "state": status.state},
            )
        package_dir = (job_dir / (status.package_dir or "")).resolve()
        target = (package_dir / path).resolve()
        # 路径穿越防护：resolve() 后目标必须严格落在 package_dir 内（且不是 package_dir 本身）
        if not target.is_relative_to(package_dir) or target == package_dir:
            raise HTTPException(status_code=400, detail="invalid path")
        if not target.is_file():
            raise HTTPException(status_code=404, detail="file not found")
        media = _CONTENT_TYPES.get(target.suffix, "application/octet-stream")
        return FileResponse(target, media_type=media)

    return app


app = create_app()
```

`Path.resolve()` 先把 `..` 归一化，再用 `is_relative_to(package_dir)` 判定——绝对路径、`..` 逃逸、`samples/../../x` 归一化后都会落在 package_dir 外而被 400 拦截（`is_relative_to` 需 Python 3.9+，requires-python 3.11 满足）。

Update `apps/api/lmdj_api/__init__.py`:

```python
from lmdj_api.app import create_app
from lmdj_api.executor import JobExecutor

__all__ = ["JobExecutor", "create_app"]
```

- [ ] **Step 4: 跑测试确认通过（含回归）**

```bash
.venv/bin/python -m pytest tests/ -q
```

Expected: `10 passed`（executor 3 + app 7）。

- [ ] **Step 5: 更新 apps/README.md**

在 `apps/README.md` 末尾追加：

````markdown
## App API 本地验证

```bash
cd apps/api
python3 -m venv .venv
.venv/bin/pip install -e ../../packages/core-models -e ../../packages/patchify -e ../../workers/audio
.venv/bin/pip install -e ".[test]"
.venv/bin/python -m pytest tests/ -q
```

真实端到端（需先 `scripts/dev.sh setup-demo`）：

```bash
.venv/bin/uvicorn lmdj_api.app:app --port 8000
# 另一个终端：
curl -F file=@song.wav localhost:8000/uploads          # → {"job_id":"...","state":"queued"}
curl localhost:8000/jobs/<job_id>                       # 轮询到 completed
curl localhost:8000/jobs/<job_id>/patch                 # patch.json
```
````

- [ ] **Step 6: 全量测试确认通过**

```bash
cd /Users/endaye/Projects/lmdj/apps/api
.venv/bin/python -m pytest tests/ -q
```

Expected: `10 passed`。

- [ ] **Step 7: Commit**

```bash
cd /Users/endaye/Projects/lmdj
git add apps/api/lmdj_api apps/api/tests/test_app.py apps/README.md
git commit -m "feat(api): fastapi routes for upload, status, and patch artifacts"
```

---

## Verification

全计划验证（Task 2 之后）：

```bash
cd /Users/endaye/Projects/lmdj/apps/api
.venv/bin/python -m pytest tests/ -q     # expected: 10 passed
.venv/bin/pip list | grep -i torch || echo "no torch ✓"
```

真实端到端冒烟（**需 demo venv**）：

```bash
cd /Users/endaye/Projects/lmdj
scripts/dev.sh smoke   # 幂等：确保 demo venv + testsong/input.wav
apps/api/.venv/bin/uvicorn lmdj_api.app:app --port 8000 &   # 后台起服务
sleep 2
JOB=$(curl -s -F file=@references/demos/lmdj-song-pipeline/output/testsong/input.wav localhost:8000/uploads | python3 -c "import sys,json;print(json.load(sys.stdin)['job_id'])")
until curl -s localhost:8000/jobs/$JOB | grep -q '"state": "completed"'; do sleep 2; done
curl -s localhost:8000/jobs/$JOB/patch | python3 -m json.tool | head -5
kill %1
```

预期：patch.json 输出 `"schema": "lmdj.patch.v1"`；产物写入 `apps/api/jobs/`（gitignored）。

## Out Of Scope For This Plan

- 队列（Redis/RQ/云托管）、Postgres/对象存储、账号/鉴权/限流。
- `POST /ideas`、generation 入口、remix/sample/fork/share/lineage 端点、cancel 实现。
- Web 侧接入 API（单独 web 小计划）。
- 部署/容器化/生产 CORS 收紧。

## Self-Review

- Spec 覆盖：executor 后台线程 + 串行锁（Task 1）；`/health`、`/uploads`、`/jobs/{id}`、`/patch`、`/files/{path}` 五端点（Task 2）；404/409/400 错误语义 + 路径穿越（Task 2 测试）；CORS localhost:5173（Task 2）；create_app 依赖注入 + FakeRunner 零 demucs（Task 1/2 conftest）；gitignore + README（Task 1/2）——逐条可指到任务。
- 类型一致性：`JobExecutor(runner, jobs_root)` / `submit(audio_path, job_id)` / `wait_idle` 在 Task 1 定义、Task 2 通过 create_app 使用；`create_app(runner, jobs_root)` 签名 Task 2 内自洽；`read_status(job_dir).to_dict()`、`status.package_dir`、`status.state` 均为 audio worker 既有 JobStatus 字段（本会话已实现并验证）。
- 已知取舍：上传临时文件用 `tempfile.mkdtemp()` 不主动清理（v1 可接受，进程级临时目录；jobs_root 内的 input 拷贝由 process_job 负责，才是持久产物）；`/patch` 与 `/files` 的 409 用 JSONResponse 而非 raise HTTPException（为在 body 带 state 字段）；串行锁测试用 threading.Event 屏障（TestClient 后台线程真实并发，barrier 观察）。
