# Resume verification of an existing candidate snapshot

Delivery base: 31e1522222881e68c87c029fad729690ef69cfc3.
Original Task: f74ece1fc5decc97a837c1891d6e89ab507bce65 (base
80778e421381be17edd7da1f0e813d10f30c0cfc). Preserve its verification below
as historical evidence, not results on this delivery base.
Integrate the canonical versions-alias correction from
fc447d4962e5153303188368bf7543ee2f572acc in the same recovery behavior:
the active docs-site versions path is a tracked compatibility symlink.

## Task and files

Expose scripts/docs-site.sh resume-version PRODUCT_BUILD CHANNEL SOURCE_SHA.
This closes the official-generator seam where a complete uncommitted snapshot
survives a failed or interrupted final check but version rejects its existing
versions.json entry. Resume verifies existing bytes, never calls Docusaurus
docs:version, copies diagrams, recreates metadata or allocates another Build.

Declared files:

- scripts/docs-site.sh
- apps/docs-site/scripts/resume-version.mjs
- apps/docs-site/scripts/lib/version-docs.mjs
- apps/docs-site/test/version-docs.test.mjs
- apps/docs-site/test/snapshot-provenance.test.mjs
- apps/docs-site/docs/operations/version-and-release.mdx
- docs/plans/2026-09-13-release-snapshot-resume.md

Require the explicit original source SHA to still equal HEAD, original channel,
schema 2 metadata, repository facts, and exact versions inventory derived from
the original source plus this Build. Reuse the complete provenance verifier,
including actual generated bytes and the exact precommit dirty boundary. Run
the full Portal check and repeat verification afterward, preserving original
metadata bytes and freeze time. This is not a committed or released candidate.

The parent still owns authorization, trusted environment/control, exclusive
workspace lease, durable request/binding, official generator invocation, final
cut commit, reviewed PR/squash, witness and complete-target CI. Partial or
drifted output is a retained failed gate, not permission to erase and regenerate.

## Verification

Unit lifecycle assertions and actual temporary Git snapshot provenance tests:
successful resume, failed final check followed by resume, missing diagram,
wrong HEAD/channel/schema, historical versions drift, and post-check mutation.
Run both Node suites, staged ownership, shell syntax and full Portal check.
No real Product Build or snapshot allocation, remote write, signing or deploy.

### Historical original-stack evidence and limits

- Initial two Node suites: 35/35 passed, exit 0;
  /tmp/lmdj-snapshot-resume-tests-v1.log. Two additional real shell entrypoint
  tests then passed with the complete version-docs suite, 17/17, exit 0;
  /tmp/lmdj-snapshot-resume-entry-tests.log.
- Independent release_journal_review inspected the final seven-file diff and
  independently ran both final Node suites, 37/37 passed; no actionable finding.
- Staged ownership: 74/74 passed, exit 0;
  /tmp/lmdj-snapshot-resume-scope.log. Shell syntax check passed.
- Node 22 scripts/docs-site.sh check passed, exit 0, including 151 tests,
  production build and 47 routes/internal links; log:
  /tmp/lmdj-snapshot-resume-portal.log. The actual generated operations page
  contains the new resume-version command. This is the current Portal check,
  not a real candidate freeze or release.
- The temporary Git bridge uses the actual diagram freezer, metadata generator
  and provenance verifier, with a two-page fixture and passive fixture facts.
  It copies fixture documents/sidebar instead of invoking Docusaurus; its full
  check callback is simulated. Those tests do not establish production snapshot
  generation, uninterrupted service ownership or real release acceptance.
- Existing freeze behavior still rejects incomplete retries; only already
  complete uncommitted snapshots admit verification-only resume. Partial-output
  reconciliation and parent durable orchestration remain explicit open work.
- Pitfall disposition: the recovery invariant is captured directly by regression
  tests. Existing post-squash witness obligations remain unchanged; no new
  process-only recurrence or ledger entry was introduced.

### Delivery-base verification

- Current source files matched the original Task parent before adaptation;
  the Portal update preserves the newer atomic/raw source-installer description.
  This Task changes no ownership policy, timeout, test selection or release gate.
- Before the canonical-alias correction, the actual temporary Git alias case
  failed with `Unexpected token '.', "../archite"... is not valid JSON`:
  1/1 failed, zero skipped, exit 1;
  `/tmp/lmdj-snapshot-resume-delivery-alias-red-v1.log`.
- With the correction, both complete Node suites passed: 38/38, zero skipped,
  33.196s, exit 0; `/tmp/lmdj-snapshot-resume-delivery-tests-v1.log`.
- Two additional real-Git negative cases cover a different alias target and
  a chained canonical target. Each asserts the exact source-alias refusal,
  no check invocation, unchanged metadata bytes, worktree status and HEAD.
  Both complete final Node suites passed: 40/40, zero skipped, 34.686s,
  exit 0; `/tmp/lmdj-snapshot-resume-delivery-tests-v2.log`.
- Staged ownership/admission passed for the exact seven declared files:
  74/74, 6.198s, exit 0;
  `/tmp/lmdj-snapshot-resume-delivery-scope-v1.log`. Shell syntax and staged
  diff checks passed. Production resume source and existing version tests are
  byte-identical to the original stack after its canonical-alias correction.
- Node 22, locked `npm ci`, and `scripts/docs-site.sh check` passed, exit 0:
  159/159 tests, zero skipped, production build and 47 routes/internal links.
  Logs: `/tmp/lmdj-snapshot-resume-delivery-deps-v1.log` and
  `/tmp/lmdj-snapshot-resume-delivery-docs-v1.log`. The actual built operations
  HTML contains the resume command, no-regeneration and alias/parent boundaries;
  explicit rendered-output check passed, exit 0:
  `/tmp/lmdj-snapshot-resume-delivery-rendered-v1.log`.
- Independent `/root/release_journal_review` reviewed all seven files with no
  actionable finding and independently ran version-docs 17/17, zero skipped,
  exit 0. The later two alias negative cases were separately inspected and
  independently passed 2/2 in 1.348s, exit 0. This is local independent review,
  not owner adoption, live PR eligibility or production release acceptance.
- The remaining full-generator gap will be closed by the subsequent candidate
  Portal journey using an isolated fixture repository, actual Docusaurus freeze,
  stable resume entrypoint and far-side frozen-output checks. The current
  fixture's simulated full-check callback does not discharge that gap.

## Version Management

Version impact: none

Reason: verification-only tooling; fixture snapshots are temporary, not release
or team-testing Builds.

## Documentation Impact

Documentation impact: required

Affected portal pages: /operations/version-and-release/
