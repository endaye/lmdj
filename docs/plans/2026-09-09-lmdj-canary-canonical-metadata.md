# T2f — Canonical Product metadata proposal

Depends on Host preparation and the merged Host changelog projection (#1024).
Compose those producers with the existing version/compiled Assembly/lock and
Runtime identity generators. Return exact before/after file witnesses; never
write the caller's repository or allocate a real Product Build.

## Declared files

- `tools/canary/metadata_proposal.py`
- `tests/build/ci_canary_metadata_proposal_test.py`
- This plan.
- `docs/plans/2026-09-09-lmdj-result-driven-delivery.md` (progress/evidence only).

The caller proposes an explicit increasing BUILD on the existing product line,
PATCH zero. This is not an occupancy decision or authorization. Product-only
changes can retain both Host versions. Docs-only/no-change allocation selection
belongs to the existing site planner and later coordinator; this function is not
an event trigger and cannot select a cut by itself.

Recollect the exact Git assessment input and Host proposal. Materialize only
bounded canonical-generator data dependencies from the pinned target into an
owned temporary directory. Never execute candidate Git code. Load installed
trusted generators in fresh namespaces and bind their source bytes to the
assessment control. A changed or unavailable generator fails closed. Retain
the canonical verifier's revision lookup through an exact pinned-commit reader
against the source Git database, while its file reads use scratch data. Do not
invent a scratch commit or consult mutable caller HEAD. Check archived bytes
against their Git blob objects; archive substitution is not a valid preimage.
Preserve independent Host versions and unmodified Assembly fields; regenerate compiled
Assembly/lock and both Runtime identity representations, then generate and
check static Host changelog pages. Verify the canonical generators' outputs
before returning the closed edit inventory and complete byte/hash/mode witnesses.

Snapshots, five-axis occupancy, lease/fencing/three-cut limit, version PR,
post-squash witness, current candidate test/artifact evidence and live execution
remain mandatory later work. No tool in this Task pushes, commits, tags, signs,
publishes, deploys or invokes a model. Missing evidence is not admission.

## Verification

Start with a missing-module regression. Real temporary Git histories and actual
canonical generators prove independent Host bumps, adequate pre-bumps,
Product-only metadata, version/Assembly/lock/compiled identity agreement, runtime
JSON/MJS consistency and rendered-log freshness. Reject wrong control generator,
missing/symlinked canonical inputs, backwards/other-line/nonzero-PATCH proposals,
blocked assessment and stale contexts. Dirty caller files remain byte-identical;
deterministic replay returns identical witnesses. Source/provider files are read
as data, not executed. Verify generator failures produce no partial proposal.

Run the focused suite, canary suites and full CI-contract discovery; staged
new-file ownership, Portal check, final range/declarations and current-head
review under issue-done. No threshold or required PR gate changes.

## Version Management

Version impact: none. Only internal proposal tooling and scratch-fixture identities
change; active Host/Product/Assembly/Contract versions and snapshots do not.

## Documentation Impact

Documentation impact: none
Reason: no active operator command/workflow or Portal behavior changes; the
internal candidate proposal is not allocation, snapshot or delivery admission.
