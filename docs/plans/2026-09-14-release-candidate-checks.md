# Executed candidate cut Task checks

Delivery base: 8987c0b27336c79462997f66050bfa936b4fa1e6.

## Task

Close the missing producer for the candidate PR's Task evidence digest before
composing the candidate parent. Run the existing three Task commands in both
staged and committed cut phases, comparing whitespace against the original base.
Consume the concrete source/snapshot/cut under its original writer; bind source,
cut, snapshot history, control revision and trusted PATH. Persist enrollment and
each intent before the actual bounded-memory subprocess; record actual exit,
output length and digest before post-command authority/source confirmation.
Only six confirmed commands produce the candidate PR evidence digest.

Cold recovery continues only commands never started. Failed, unknown or
unconfirmed commands cannot replay; missing enrolled history cannot reset its
budget. A committed cut without staged command history cannot manufacture that
history. Passive PR verification authenticates the actual private binding and
command history, not source acceptance, review, main authority or release CI.

Add an exclusive guarded callback to the concrete cut installer, preserving the
existing trusted callback interface. Guard the retained source/snapshot/cut and
exact staged/committed checkout before and after each command and before the ref
update. Reuse the existing process executor and its inherited original writer
descriptor. Keep candidate history separate from witness history; do not change
the previously accepted witness command protocol in this narrow Task.

## Declared files

- tools/release/candidate_checks.py
- tools/release/candidate_cut.py
- tests/build/release_candidate_checks_test.py
- tests/build/release_candidate_portal_journey.py
- CMakeLists.txt
- apps/docs-site/docs/operations/version-and-release.mdx
- docs/plans/2026-09-14-release-candidate-checks.md

## Verification

Lowest tier: contract. Use actual source/snapshot/cut producers and real child
processes with labelled small Portal/ownership fixture entrypoints. Verify six
executed results and passive PR binding, cold no-replay, command failure/death,
lost final authority, source/history/checkout drift, missing state, rebound
commands/control and post-ref-update recovery. Keep existing test and command
budgets unchanged; new individual cases have an initial 60-second budget.
Run existing cut and snapshot regressions and the existing process-lock tests.
Run locked Node 22 Portal checks and rendered-page assertions. Replace the full
Portal rehearsal's hand-driven cut callback with this concrete runner, preserve
every snapshot/squash/witness/fresh-clone leg, and assert six persisted results
and a cold resume with identical history and zero new commands. Retain a fresh
complete rehearsal and raw failures; fixture unit success does not replace it.
Before local commit run staged ownership/admission and whitespace, inspect only
declared files, and obtain independent complete-diff and exact-head review.

The dependent stack remains local/unpushed pending upstream PR #1296 owner
adoption. No GitHub business write, provider call, release/signing/deployment,
host/auth/key/protection change, cleanup or invented owner attestation. This is
one prerequisite of the full goal, not completed production orchestration.

## Version Management

Version impact: none

Reason: internal command evidence only; no Product/Assembly, public Contract,
Build allocation, snapshot, tag or release changes.

## Documentation Impact

Documentation impact: required

Affected portal pages: /operations/version-and-release/

Reason: document concrete cut-command evidence and the remaining production
parent, review and release-acceptance boundaries.

## Execution evidence

The six non-plan files are bound by
`/tmp/lmdj-candidate-checks-tested-files-v2.sha256`. These local development
results are not owner adoption, remote review or release acceptance.

- Configured CMake uses Python 3.14.7; Node/Portal commands explicitly prepend
  Node 22.22.2. Configuration, locked installation and current Portal check
  returned 0. Portal: 170 tests passed, zero failed/skipped; 47 routes and
  internal links valid. Four assertions against the actual rendered operation
  page passed. Logs: `/tmp/lmdj-candidate-checks-configure-v1.log`,
  `/tmp/lmdj-candidate-checks-install-v1.log`,
  `/tmp/lmdj-candidate-checks-docs-v1.log`,
  `/tmp/lmdj-candidate-checks-rendered-v1.log`. Installation reported 27 existing
  dependency vulnerabilities; no dependency or lockfile was upgraded here.
- Existing process-executor regressions passed 26 tests, exit 0, including real
  orphan-child writer fencing: `/tmp/lmdj-candidate-checks-executor-v1.log`.
- Prefinal configured CTest v1 returned 8, 15/16 groups passed. The cut safety
  group ran seven cases and failed only the existing retention-conflict
  diagnostic assertion: the newly added pre-guard was inside callback-error
  sanitization. Preserve `/tmp/lmdj-candidate-checks-ctest-v1.log` and complete
  `/tmp/lmdj-candidate-checks-ctest-v1-LastTest.log`. Self-review identified the
  cause; internal pre/post guards now retain their safe diagnostics outside
  callback sanitization. Callback exceptions remain sanitized. The same real
  retention case then passed, exit 0, 9.477 seconds:
  `/tmp/lmdj-candidate-checks-retention-v2.log`.
- Final configured CTest v2 returned 8, 15/16 groups passed: all 12 new cases,
  five old cut recovery cases, seven cut safety cases and ten snapshot safety
  cases. The unchanged snapshot recovery group timed out at 30.04 seconds,
  with no reported case output. Do not claim its four cases passed or infer a
  cause from the prefinal group's passing result. Complete raw evidence:
  `/tmp/lmdj-candidate-checks-ctest-v2-LastTest.log`; console:
  `/tmp/lmdj-candidate-checks-ctest-v2.log`. After the complete rehearsal ended,
  an isolated same-budget follow-up passed all four recovery cases, exit 0,
  27.64 seconds: `/tmp/lmdj-candidate-checks-isolated-v1.log`. Thus all 16
  selected groups (38 cases) have passing final-code evidence across v2 and
  this explicit follow-up; neither earlier batch's exit 8 is reclassified.
- Initial staged ownership/admission passed 74 tests, exit 0:
  `/tmp/lmdj-candidate-checks-scope-v1.log`. Complete-diff independent static
  review confirmed the guard repair and six-file manifest. The final staged
  ownership check is retained in `/tmp/lmdj-candidate-checks-scope-v2.log`.
  Commit handoff must preserve these exact tested bytes and receive exact-head
  independent review.

The first Portal rehearsal was deliberately interrupted during initial snapshot
generation after discovery of the diagnostic regression. Its controller returned
1 and original generation remains unconfirmed (`result: null`). It is not a
spontaneous product-failure claim and will not be resumed. Retain
`/private/tmp/lmdj-candidate-checks-portal-v1.4gCfUX/run-v1`, outer
`/tmp/lmdj-candidate-checks-portal-v1.log`, and the explicit process/signal
disposition `/tmp/lmdj-candidate-checks-portal-v1-disposition.md`. No retained
rehearsal fixture or historical worktree was deleted. The separate fresh,
final-code rehearsal in
`/private/tmp/lmdj-candidate-checks-portal-v2.qkA9I5/run-v1` completed with exit 0;
outer log `/tmp/lmdj-candidate-checks-portal-v2.log`. It retains actual source
installation, official snapshot generation/resume, six cut checks and cold cut
with unchanged commit/history and zero new commands, actual introducing squash,
official witness generation/cold verification, six witness Task checks and cold
Task with unchanged history and no replay, witness squash and source-absent
fresh-clone Portal verification. Final `logs/019.log` reports 170/170 tests,
zero failures/skips, 47 routes and internal links valid. This proves the stated
local fixture journey, not production release acceptance.

External read-only `/tmp/lmdj-candidate-checks-audit.py` retains all previous
far-side and 17-spool assertions and additionally checks the six exact cut
command vectors, original source/cut/snapshot identities, canonical private
history/enrollment and cold-resume receipts. Independent static review and actual
auditing of the complete v2 evidence both passed, exit 0:
`/tmp/lmdj-candidate-checks-audit-v1.log`. The independent reviewer also executed
this audit successfully. It authenticates 21 outer commands (19 exit 0 and two
expected source-absence exit 1), all 17 actual output spools in their 2/6/3/6
producer populations, complete command vectors, original emitted witness bytes,
actual Git objects/trees/squashes and unchanged controller/harness hashes.
It also refused the incomplete v1 run and Python `-O`, each exit 1, in
`/tmp/lmdj-candidate-checks-audit-incomplete-v1.log` and
`/tmp/lmdj-candidate-checks-audit-optimized-v1.log`.

The rehearsal's recorded base/control revision is
`8987c0b27336c79462997f66050bfa936b4fa1e6`, with actual WIP controller and harness
bytes separately hash-bound; this does not claim the new controller already
existed at that base. Fixture source is
`800236ac35074d4cf735efa4c5899aa7374118f8`, cut
`6abc416c73b2d770c024a65c2e977fcf84555e73`, introducing squash
`f6ad3387116f992930c9c49a55f6259b5719dd4d`, witness Task
`89282f0add34526746b392d5a6804837a145e726` and witness squash
`99b81b6fa64412ef28c761e002072c59a7ff2d46`. Cut command proof is
`76818cc46dae53b09ea5ee0a040ff8a6c99e9b663bcb48f93a57e21535f94d7a`.
The fixture Build `1.0.57.0` is not a real Product allocation. Production parent
composition, remote review, complete CI, signing, publication, live changelog,
dual Host deployments, promotion and full release acceptance remain outstanding
in the original single-command-release objective.

Pitfall disposition: no new ledger entry. The diagnostic regression is already
causally expressed by the existing retention test and fixed without weakening
it; the separate timeout remains evidence, not a new inferred process rule.
