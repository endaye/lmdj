# T4 immutable staged deployment revisions

## Current owner scope and J1–J4 correction

The owner withdrew proactive balance/native-funding admission from this wave
on 2026-09-11. That withdrawal overrides historical funding prerequisites below;
do not import the unfinished native-funding branch. Supplier warning #1188 is
deferred, not a dependency. Existing merged request/count/USD safeguards remain.
Real host, credential entry, four-provider, quality, cutover and handoff evidence
remain required for the migration and are not established by this source Task.

Independent full-Task review rejected
`2f69d7c5db5b621e5f1d2d9a62499c14fbcc45dd`. Correct exactly J1–J4 within the
same five declared files before amending the unpublished Task commit:

1. Reject a unique-ID install from A to the same immutable A during terminal
   history authentication, before normal/recovery/idempotent effects. Retain
   byte-identical same-ID duplicate recovery and valid six-arrival history.
   Test a real initial stage, ordinary repeat and unavailable rollback, then
   an otherwise canonical new-ID A-to-A arrival; stage and rollback must refuse
   with intended diagnostics and unchanged complete snapshots.
2. Bind every six-leg far-side service/slice byte expectation and receipt
   identity/target to independently declared A/B inputs, not mutually agreeing
   output copies. Actual repository methods must detect final stored+external
   unit corruption and coherent wrong receipt target immediately, without a
   subsequent installer invocation.
3. Require actual REVISION_RECORD.json content/final-metadata file fsync before
   publishing each new A/B release directory. Derive expected A/B identities
   independently. Compare final device/inode/mode/UID/GID with each applicable
   published identity after every leg. Actual record-fsync omission and a final
   same-byte/mode/owner replacement on a new inode must fail the durable method.
4. Snapshot the complete relocated external receipt tree as well as target in
   the nested-parent refusal test. Prove a post-refusal external receipt change
   fails that actual repository method while preserving its intended denial.

First reproduce the reported failures, then retain focused regressions and all
existing journey legs. Use the independent report and raw fault programs to
verify the real methods, not a replacement assertion implementation. Rerun
normal/no-site/umask0002/child-no-site runner matrices, pinned v14 zero-skip
integration, adapter, full scope, shell syntax and docs-site checks. Update
operations and portal proof claims to exactly match evidence. No thresholds,
timeouts, fixture trust, old negative assertions or acceptance scope shrink.
Commit only after complete Task verification; rerun exact-head docs afterward.
A fresh full-Task independent review is required before push or host effects.

The lead rejected the subsequent `dde3b793a0ff74d529ee99202f0936351b9c171c`
amendment: actual N1 still accepts coherent final service/slice plus record and
receipt commitment corruption, and actual N3 still accepts record fsync after
release rename and parent fsync. The reported 90-test passes do not close J2
or J3. Require independent A/B unit bytes, invoking/from inventory for rollback
receipt targets, and nested record fsync before the applicable publication.
The real-method coherent-unit and late-record-fsync fault probes must fail
immediately on the affected leg, while all normal six-leg journeys still pass.
Preserve both rejected candidates and their evidence; do not relabel the
author's completion report as acceptance.

Version impact: none — unpublished CI operator-state/test corrections only.
Documentation impact: required
Affected portal route: `/operations/testing-and-proof/`.

Final proof also reproduces malformed receipt objects being silently ignored:
empty objects and v3 records with absent/null transition IDs must be rejected
read-only by the closed deployment stream, never excluded from replay. Add
focused actual-object/full-snapshot regressions, preserve byte-identical
duplicate and interrupted-writer recovery, and rerun every Task gate after the
source fix. This does not authorize legacy migration or monetary-ledger edits.

Parent: umbrella #1149 / T4 #1153. This implementation prerequisite supports
an honest inactive installation followed by a same-bundle configured deployment
with complete reversible nonsecret configuration identity. It does not claim
final deployment acceptance.

## Outcome and boundary

Implement one coherent deployment-revision transition while keeping the engine
and detached v14 identity unchanged. A future pricing or model configuration
update must not require falsifying or rebuilding the engine bundle. This Task
is source-only: no SSH/admin or host mutation, GitHub writes, secrets, API or
production calls, new runner, product Release, credential workflow, generic
controller, or migration master-plan edits.

The declared files are:

- `scripts/ci/pr-agent/deploy-runner.sh`
- `tests/build/ci_pr_agent_runner_test.py`
- this plan
- `docs/quality/2026-09-10-pr-agent-netcup-operations.md`
- `apps/docs-site/docs/operations/testing-and-proof.mdx`

`scripts/ci/scope_policy.json` is changed only if this new plan is not covered
by an existing ownership/full rule. The existing `.md` rule and
`scripts/ci/` full rule are expected to provide that coverage.

## Second correction contract (R1-R4)

This correction preserves the prior F1-F7 fixes and all positive journeys. R1
binds every established-state witness to the actual last canonical terminal
receipt in append order; only the exact pending receipt may be the one extra
arrival in an interrupted receipt-before-state window. R2 authenticates every
retained immutable release and recorded operator path, including ownership,
mode, secure parents and complete detached/member identities, before any
receipt, transition or file mutation; checks use the intended far-side state.
R3 binds install receipt endpoints exactly to transition `from`/`to` and
validates the complete prospective terminal state before append or clear. R4
requires `current` to be the exact canonical literal symlink target, rejecting
unrecorded intermediate aliases without repair. Malformed, stale, forged,
conflicting, wrong-owner, wrong-mode and wrong-length evidence remains
read-only and retains pending state; direct links, absent-old-path rollback,
renderer drift rollback, duplicate-identical receipts and the full extended
journey remain supported.

## Contract

### S1-S6 correction contract

The independent review of `031cad4c75ed68fada762db230abe5eb7cde6085`
rejected six remaining defects. Pending state always retains its prior
historical witness: no admitted writer stores the pending receipt as that
witness. Preserve the actual writer's receipt-before-final-state crash window,
but reject a pending transition that presents its own receipt as prior history.
Validate the entire canonical terminal stream for conflicting transition IDs;
identical repetitions represent one arrival and remain supported, including
pending recovery. Bind receipt target/archive/endpoints to trusted inventory
and retained release identity where applicable, not merely agreement between
state and another copy of the same receipt.

Before every normal or recovery effect and idempotent success, validate the
secure ancestors of every retained immutable release and external operator
path. Ordinary new installation cannot skip the old previous path merely
because it is about to replace that retained record. A literal current symlink
does not permit a symlinked releases parent or an insecure destination chain.
Retain the specifically supported absent-old-path rollback restoration and
phase-aware shared-path byte expectations.

Correct the stale-history and pending-rollback-mode tests to reach their named
guards with otherwise valid state. Require the intended failure reason and
complete unchanged snapshots; add separate actual unsafe-parent tests instead
of naming an owner-only case as parent coverage. Port all sixteen independent
scenarios, including actual-writer crash and six-transition inode-aware
durability, without dropping prior F1-F7/R1-R4 or positive journeys. Refresh
Portal verification against the settled final commit before shipment.

### Stage

Add explicit `stage`, requiring inactive deployment and strictly typed boolean
admission fields, protected supplied runtime TOML with non-null SHA/length, and
the existing operator/service identity boundary. Stage does not require
post-install isolation, capacity, or live activation claims; it requires
operator-access and runner-classification prerequisites without fabricating
pending proofs. Every provider entry in the stage TOML must be disabled,
validated by the unchanged adapter trusted-config loader or a safe equivalent
that rejects any enabled entry before effects. Input is never imported or
executed.

Stage creates the same immutable release and controlled state/unit paths through
the existing pristine/identity boundary. The staged service must be unable to
execute the engine even if started: use an explicit fixed fail-closed unit
guard, rendered and identity-bound. Stage invokes no systemctl, credentials,
or API; probe/launch/admission remain separate. Existing `install` continues
to require active config and all prior admissions, now strictly typed.

### Immutable deployment revisions

Replace archive-only release addressing with a deterministic immutable revision
derived from canonical nonsecret archive, detached identity, runtime config,
operator config, and deployment-tool/rendering identity. Do not hash a
self-referential unit/path. Same bundle plus different authenticated runtime or
activation inventory produces a distinct revision; unchanged complete input is
idempotent. Keep original archive/member identity fields, full byte lengths and
digests. Each revision contains protected exact operator JSON and runtime TOML
copies with complete identities, and no secrets.

Each revision also retains the exact rendered `service.unit` and `slice.unit`
bytes, their SHA-256/length identities, and the deployment-tool identity;
rollback reads those authenticated stored bytes instead of rerendering with
today's tool. Before a new intent, the runner authenticates the current pointer,
current and retained previous releases, every detached member, and every
recorded operator/runtime/unit file including owner, mode, parent chain and
byte identity. Runtime state and deployment receipts use closed unpublished
v3 shapes, including an exact latest-transition witness; legacy or malformed
records fail read-only and are never silently migrated.

Authenticate current deployed operator/runtime files against current state
before replacing them. New config authorizes desired bytes, not old bytes.
Rollback restores prior operator JSON, runtime TOML, unit bytes and current
pointer as one authenticated transition, using recorded prior inventory to
render/verify prior units. Closed state schemas reject malformed, forged,
incomplete, extra-identity and wrong-length records. Existing v2 state either
migrates through a fully verified explicit path or is rejected read-only with a
precise remedy; identity is never inferred. A real host has no installed state
yet.
This implementation emits the v3 closed runtime-state discriminator and
refuses legacy v2 state read-only with a precise recovery remedy; it never
infers missing v3 identities.

### Preserved safety and recovery invariants

Preserve fail-closed missing/corrupt state and uncertain-transition retention,
canonical same-ID receipt equality, old/new file and unit ownership checks,
safe archive extraction and source identity, unique per-transition IDs, no
symlink substitution or unsafe path repair, and complete control-transition
serialization with an operator-owned lock. Use a safe documented creation
boundary so a rejected pristine check never appears partially installed.
Persistent state, receipt, directory, and file replacements fsync file content
and final metadata before rename, then fsync the containing directory before
reporting durable success.
Do not broadly delete, implicitly repair, or truncate the monetary ledger.
Interrupted transitions reconcile only from authenticated exact far-side evidence
or stop with retained data; tests must not skip interrupted cases.

Copied source/runtime/release files remain protected and runtime writable paths
remain separate. Preserve one-slot/1CPU/2Gi limits and heavy 14CPU/48Gi limits.
Rollback never restarts or asserts health. A first install has no previous
target: report unavailable and retain evidence. Stage cannot enable a previous
paid service or inherit an old launch admission; identity changes invalidate
later externally bound admission.

## Implementation Tasks and tests

1. Update the runner with explicit inactive `stage`, canonical complete
   deployment revisions, closed state/receipt identities, authenticated current
   file replacement and complete rollback, while retaining v14 archive and
   resource/security/recovery/monetary invariants.
2. Add behavioral regression coverage for disabled A stage -> enabled B install
   using the same archive -> rollback A -> reinstall B, asserting every
   operator/runtime/unit/current/state identity at each far side, idempotence,
   immutable previous revisions, and unchanged uncertain monetary records.
3. Cover negative enabled stage TOML, nonboolean admission, missing runtime
   identity, writable/symlinked/forged old config, malformed state, mismatched
   previous lengths/digests, concurrent mutation, and interrupted config/unit/
   pointer updates. Synthetic activation flags are fixture-only and make no
   live admission claim.
4. Run existing runner tests, normal and `0002` umask, `python3 -S` where
   supported, bash syntax, actual v14 opt-in archive verification without paid
   calls, new-file ownership, and full docs-site verification. Do not lower
   timeouts, skip journey legs, or replace behavior with source-string checks.

The specified journey is: disabled A stage, enabled B install with the same
archive, rollback to A, then reinstall B. Every transition must have a
far-side assertion for the full file/digest/length identity set, plus explicit
retained evidence for first-install unavailable rollback and interruptions.

## Operational and acceptance boundary

### N1-N3 independent review correction contract

The unpublished `62fe2a03cc12630ec93fd7b96753af0929f48dd5` candidate was
rejected: individually existing endpoints and same-parent target inventories
do not authenticate historical arrivals; state/receipt leaf trust and nested
receipt ancestors were unchecked; promised regression ports were incomplete.
Before new acceptance, validate unique first-arrival history continuously from
the initial null source, bind install target to its exact immutable destination
inventory and rollback to its invoking semantics, and authenticate every
historical endpoint/prior witness. Existing state/receipt files require trusted
operator ownership and 0600 with secure parent chains before reads or effects;
never repair unsafe evidence. Preserve legitimate receipt-before-state crashes,
duplicate-identical records and all six same-archive transitions.

Port all16 previously specified independent scenarios into durable repository
tests, including actual writer crash and six individually reset inode/fsync
traces. Add focused historical-target/endpoint and control-file/ancestor probes;
the far-side rollback test must assert its intended owner/mode denial as well
as unchanged complete state. The lead records this contract before the N1-N3
implementation dispatch. Full existing suites, umasks, child-S, actual v14 and
final exact-head Portal verification remain required; no runtime gate shrinks.

Operations documentation removes the invalid preplacement recipe and replaces
it with honest supported source-tool steps plus explicit not-ready probe,
credential, and launch portions without guessed executable paths. Original T4
real sandbox/coexistence/recovery and supplier funding/health, T5 quality/cohort
and four-provider acceptance, and T6 remain required. Candidate remains
deepseek-flash; the USD1 attempt and USD20 pilot/month limits are unchanged.
Existing GitHub Secret reuse belongs to a separately reviewed secure injection
flow. Administrator scripts are handed to the owner when reviewed, never
executed by this source Task.

## Version Management

Version impact: none — internal CI deployment state schema only; no Product,
Module, Provider, Contract, or original bundle identity changes.

## Documentation Impact

Documentation impact: required
Affected portal pages: /operations/testing-and-proof/
Reason: staged revision lifecycle, complete rollback, and explicit pending
operational evidence.

## Isolated history correction before source acceptance

The original 1cc93465977be873dea9a40bc18cd6c28e7d38d2 review remains
in progress in its unchanged worktree. The lead and independent review status
have already reproduced impossible rollback history, historical record path
substitution/coherent unit forgery, invalid pending receipt publication and
weak final far-side test assertions. These findings reject the candidate;
a completed final review is not claimed. This isolated continuation is based
on fresh main 561a8ce8b003806d559b3702624133b0c91f838c; the original five
Task paths are byte-identical there to the old c910 base before Task changes.

Carry the original full staged-revision implementation into this isolated
five-file Task without altering the in-progress review target. Replay current
and previous arrival semantics, bind exact historical endpoints and complete
revision records to operator-owned receipt commitments, validate prospective
pending history before effects, and reject unsupported rollback invocation
inventories before writing. A service-owned record is not its own authenticity
anchor. Preserve historical renderer drift by authenticating stored unit bytes,
not executing an old helper or rerendering with the current one.

Retain all sixteen promised ports, all old positive/negative/crash journeys,
and independent full far-side file/identity/mode/fsync assertions at every leg.
Root-protected canonical receipt commitments extend the unpublished closed v3
receipt with `from_record_sha256`, `from_record_byte_length`,
`to_record_sha256`, and `to_record_byte_length`. The source commitment is null
only for the initial install; every non-null commitment is the SHA-256 and
length of the canonical complete `REVISION_RECORD.json` bytes. Missing or
partial commitments refuse read-only rather than being reconstructed from
untrusted historical files. Rollback invocation inventory must exactly match
the authenticated current arrival, except an exact repeated latest rollback is
a validated no-op. Actual host state is not migrated. Run full
runner suites, real v14 archive, prior probes, adapter, complete change-scope
and exact-head docs before one verified commit. No push or operational action
until a fresh complete independent review accepts the resulting exact head.
Any additional findings in the original final report remain unresolved until
incorporated and verified. Native funding and credential work are out of scope.
# H1–H4 exact-head rejection and correction boundary

Lead did not accept follow-up `1ae42831b9e439991faf14cb8ee57be084784cd8`:
N1 still omits first-A and first-B complete far-side assertions. N3 or scratch
coverage is not a substitute for those required N1 journey legs. Complete all
six immediate N1 assertions (including initial monetary absence), prove first
and final corruptions fail the actual methods, and finish old probe cases
after early assertion stops using faithful canonical fixtures. Documented
missing probe coverage is not completion. All mandatory suites and exact-head
docs must pass after those changes before the unpublished Task is amended.

Independent review rejected `ae5a37e03ee3f3461dcd193025de5db6cd8eede4` despite
passing local suites. Pending recovery must bind the prior witness to the exact
base unique-arrival history, removing only a matching final pending arrival;
null is valid exactly for an empty base. Both pending install publications and
rollback retain the exact base current/previous pair, with future previous in
transition metadata only. Recovery must cover actual initial stage and active
install receipt-before-final-state crashes exactly once, while malformed or
mixed state remains read-only refusal.

Complete immutable record commitments cover actual stored canonical bytes and
length, not merely reserialized parsed data. No historical trust learning,
record repair or host migration is authorized.

Each of six journey far sides must use independently fixed A/B expectations,
complete state/receipt/record identity and actual final mode/UID/GID. Per-leg
fsync/inode traces also check final destinations. Coherent wrong sixth-leg
restoration, final runtime corruption and final state0666 injections must fail
the real durable methods. Focused relabel/clone/mode tests must reach their
named guards; external relocated receipt trees belong in unchanged snapshots.
All original sixteen port actions and prior positive/adverse cases remain.

Scope is the existing five-file Task; no new files, host operations, funding or
credential changes. Normal, no-site, umask0002, installer-child-no-site, actual
v14 archive, adapter, change-scope, syntax and exact-head docs verification
remain mandatory. Correct reported run/pass/skip counts from raw evidence.
Amend the unpublished single Task commit only after complete verification;
fresh full exact-head independent review is required before shipping.

Version impact: none — unpublished operator transaction corrections only.
Documentation impact: required
Affected portal route: `/operations/testing-and-proof/`.
T4 real deployment, native funding and credential bridge, new Linux bundle,
T5 full quality/four-provider acceptance and T6 cutover remain uncompleted gates.
