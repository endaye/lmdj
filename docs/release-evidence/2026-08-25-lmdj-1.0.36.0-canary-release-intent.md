# LMDJ 1.0.36.0 Canary Release Intent

## Evidence boundary

This record authorizes `lmdj-v1.0.36.0` as a reviewable `canary` Product
release intent at one exact protected-`main` revision. It binds the existing
immutable Product Build and Web Runtime Host release profile to a retained full
CI run. It does not create or push a tag, create or publish a GitHub Release,
deploy Runtime assets, or promote a Channel.

The remaining hands-on and physical rows are deliberately not converted into
automated Proof. This candidate may proceed only as `canary`; it is not a
`beta`, `stable`, or complete physical-acceptance claim.

## Exact candidate binding

| Field | Value |
| --- | --- |
| Candidate tag | `lmdj-v1.0.36.0` |
| Product Build / Channel | `1.0.36.0` / `canary` |
| Release profile | `web-runtime-host` |
| Intent disposition | `releasable` |
| Exact protected-`main` target | `4a145f4aeba8594cf4b9c53cddfe8a67fabb5bf2` |
| Immutable snapshot | `apps/architecture-portal/versioned_metadata/version-1.0.36.0.json` |
| Snapshot metadata SHA-256 | `85369531b86fa6353b57dee3059f6b9f8e872a71f427aaf5ebd5a6cd0e307b79` |
| Snapshot authenticated source revision / tree | `1f0af5984560e793b1fda94e0e0e9233915984be` / `f6f857bbb180be1682d1dcb60bab2cb02569f44a` |
| Assembly Lock SHA-256 | `6011009fd624614cb2198d1c13173f9be2a7bd6e6e901e40c855a633d64948cf` |
| Full merged-main CI | [32857088479](https://github.com/endaye/lmdj/actions/runs/32857088479) |

The Product Build was introduced on protected `main` by squash merge
`d5a0af2ce2ec416eeb1dc80faeb4eb4aa6d9470d` from
[#293](https://github.com/endaye/lmdj/pull/293). That merge advanced Creator
Web Host to `1.5.5`, audio-runtime to `0.5.1`, and Product Build to
`1.0.36.0` for the Creator capture UI, input-gate, recovery, and render-path
ramp remediation.

The later target `4a145f4…` is the exact protected-`main` revision selected for
this release intent. Between `d5a0af2…` and `4a145f4…`,

```bash
git diff --quiet d5a0af2ce2ec416eeb1dc80faeb4eb4aa6d9470d..4a145f4aeba8594cf4b9c53cddfe8a67fabb5bf2 -- \
  packages providers contracts products apps/creator-web apps/web-runtime-host \
  apps/native-host apps/core-cli apps/core-mcp
```

returned success. The intervening commits affect tests, CI, packaging, Portal
projection, skills, and governance; they do not change the active Product,
Assembly, Module, Provider, Contract, Creator Web, or formal Host source bound
by this candidate. The Portal snapshot projection check also passes at the
exact target, so the earlier authenticated source revision remains valid
without a handwritten squash witness.

## Exact full-CI proof

The explicit full `Core CI` workflow run
[32857088479](https://github.com/endaye/lmdj/actions/runs/32857088479) used:

| Field | Value |
| --- | --- |
| Event / branch | `workflow_dispatch` / `main` |
| Head SHA | `4a145f4aeba8594cf4b9c53cddfe8a67fabb5bf2` |
| Created / completed | `2026-08-25T14:03:12Z` / `2026-08-25T14:25:31Z` |
| Workflow conclusion | `success` |
| Scope mode | `full` |
| Aggregate gate | `PR Gate: success` |

Its retained `lmdj.ci-scope.v2` artifact selects all 14 closed lanes:
`chameleon_lab`, `ci_contract`, `core_asan`, `core_coverage`, `core_macos`,
`core_ubuntu`, `creator`, `deploy_contract`, `docs_static`, `package`, `portal`,
`web_runtime_host`, `web_runtime_lab`, and `web_toolchain`. The terminal job
inventory is 19 `success`, 1 designed `skipped`, and 0 failed. The skipped
GitHub-hosted macOS fallback was unnecessary because the trusted primary macOS
gates succeeded.

This run is separate from the focused CI that will validate the release-intent
PR itself. After that PR is merged, `scripts/release.sh audit --remote --tag
lmdj-v1.0.36.0` must freshly revalidate canonical policy, this exact target and
run, branch protection, tag/Release absence, Environment state, and retained
scope evidence before any separately authorized transition.

## Manual and physical boundary

The current manual ledger in
`docs/quality/2026-08-17-manual-verification-todo.md` remains authoritative:

- M2 human hearing and subjective audio quality is still `unverified`. Checks
  1 and 5 are ready to rerun on `1.0.36.0`; loop-seam clicking remains expected
  until the deferred crossfade lands.
- M3 macOS Chrome Pointer has not started.
- M5 external audio interface and the current-Build M6 physical MIDI journey
  still require hands-on evidence under the carry-forward rule.
- Creator UI remediation requires the M1 usability re-walk plus M2/M3; the F4
  input-switch case and F6 hearing checks also remain outstanding.

Those open rows are visible canary risk, not a hidden release gate pass. This
intent authorizes review of a canary candidate only and preserves the manual
work as a separate decision before any later Channel promotion.

## Next transition boundary

Merging this PR would make the intent part of canonical policy; it would not
perform a release. The next possible mutation would be only
`scripts/release.sh prepare lmdj-v1.0.36.0`, after a fresh exact-tag remote
audit and separate authorization. `prepare`, `push-tag`, `create-draft`,
publication, Runtime deployment, and Channel promotion remain independent
boundaries.

## Publication record (2026-08-26)

`lmdj-v1.0.36.0` was published through the protected `publish-release.yml`
workflow, run
[32982586705](https://github.com/endaye/lmdj/actions/runs/32982586705),
after its `verify-draft` and pre-publication exact-tag remote audit both
passed with `contents: write`.

| Field | Value |
| --- | --- |
| Release ID | `376929650` |
| Published at | `2026-08-26T14:50:24Z` |
| Release URL | <https://github.com/endaye/lmdj/releases/tag/lmdj-v1.0.36.0> |
| Prerelease | `true` (canary policy) |
| Assets | `lmdj-web-runtime-host-1.2.15-product-1.0.36.0.zip` + `.sha256` + `.sha256.asc` |
| Plan SHA-256 | `81df645504e7b0171bef3e61fc42823bfacdd1ccda7e34964918c2628c0e3188` |

The workflow's own publish step reported a false failure after the successful
mutation and skipped the in-run post-publication audit: the post-mutation
snapshot comparison includes `html_url`, which publication necessarily
rewrites ([#337](https://github.com/endaye/lmdj/issues/337)). The published
state above was verified by an independent operator audit and Release read
after the run. Getting this first end-to-end publication through also required
[#331](https://github.com/endaye/lmdj/issues/331) (canonical fetch
credentials, intent hydration) and
[#335](https://github.com/endaye/lmdj/issues/335) (Draft verification moved
out of read-only preflight; spec §8.4 amendment).

Runtime deployment and Channel promotion remain unperformed independent
boundaries.
