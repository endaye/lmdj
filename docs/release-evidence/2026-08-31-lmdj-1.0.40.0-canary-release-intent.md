# LMDJ `1.0.40.0` Canary Release Intent — 2026-08-31

This record binds the Stage 9 Sequence Recording Product Build to one exact
protected-`main` revision, one immutable Portal snapshot, and one successful
full `Core CI` run. It authorizes a reviewable `releasable` ledger row only. It
does not create a tag, a Draft, a GitHub Release, a Runtime deployment, or a
Channel promotion, and it does not convert any deferred manual or physical row
into automated Proof.

## Exact release target

| Field | Value |
| --- | --- |
| Tag | `lmdj-v1.0.40.0` |
| Kind / identity | `product` / `1.0.40.0` |
| Channel | `canary` |
| Profile | `web-runtime-host` |
| Target revision | `bb0544c46d4b3fc3a7c96cb848e10cdecb4be040` |
| Target tree | `fa6eacd557aad63a4a1119c113a1608f02f3341d` |
| Snapshot | `1.0.40.0` |
| Exact-main run | [`33259586218`](https://github.com/endaye/lmdj/actions/runs/33259586218) |

The target is the squash merge of PR
[#420](https://github.com/endaye/lmdj/pull/420) on protected `main`. That PR
retained the Stage 9 remediation Task 10 version integration
(`67be108bb5c6fd77f943516d17bdfcdf4416a859`) and the Task 11 immutable snapshot
(`d191324952473d246c40402797b7fb75091d6d50`) as separate clean commits; final
PR head `d28831e6821f72adb234566049ff5a37394f9d83` and the squash merge share
the exact tree `fa6eacd557aad63a4a1119c113a1608f02f3341d`.

`bb0544c4` is an ancestor of protected `main`, so `prepare` can check it out.
The target is deliberately not `main` HEAD: later commits carry no exact-SHA
full-CI evidence, and `main` is presently red on several of them.

## Immutable snapshot and Assembly identity

`apps/architecture-portal/versioned_metadata/version-1.0.40.0.json` binds:

| Field | Value |
| --- | --- |
| `product_build` | `1.0.40.0` |
| `channel` | `canary` |
| `revision` | `67be108bb5c6fd77f943516d17bdfcdf4416a859` |
| `assembly_lock_sha256` | `848390ec5be2a1a511f5ad93c728598c84665a65353cd8d80dc59c3bc596ea01` |
| `source_commit.tree` | `b0c44989e1788bbf5be7753894eae94883c33c28` |
| `source_projection.sha256` | `d110a802bdd34488159cb0fdefb3ab0be2099d84fe985be4f6d656cbb7fc81f1` |
| `frozen_at_utc` | `2026-08-29T13:15:52.098Z` |

At the exact target revision, `products/lmdj/version.json` resolves to identity
`1.0.40.0` and `sha256(products/lmdj/assembly.lock.json)` equals
`848390ec5be2a1a511f5ad93c728598c84665a65353cd8d80dc59c3bc596ea01`, matching
the snapshot metadata. This is the condition
`tools/release/target_validation.py::validate_current_product_snapshot`
enforces.

The Product tree is unchanged between allocation and target: no commit between
`67be108b` and `bb0544c4` touches `products/`. Commits merged after the target
touch `apps/native-host/CMakeLists.txt`,
`packages/application-facade/CMakeLists.txt`, one Provider test, and one Portal
document; none touches `products/`, so the recorded Assembly identity remains
exact for this target.

Module identities in this Build: Project I/O `1.0.1`, Audio Runtime `2.0.1`,
Application Facade `2.1.1`. Build 39 already absorbed Web Runtime Platform
`2.0.1` and both Web Host `2.1.1` identities, so they are not double-bumped.

## Full exact-main CI evidence

Run [`33259586218`](https://github.com/endaye/lmdj/actions/runs/33259586218) is
a `workflow_dispatch` of `ci.yml` with an empty `lanes` input on exactly
`bb0544c46d4b3fc3a7c96cb848e10cdecb4be040`, which is the operator dispatch path
the release skill requires when a target's own push classified focused.

| Gate | Observed |
| --- | --- |
| Workflow / branch / event | `Core CI` / `main` / `workflow_dispatch` |
| Head SHA | `bb0544c46d4b3fc3a7c96cb848e10cdecb4be040` |
| Conclusion | `success` |
| Same-run `Change Scope` | `success` |
| Same-run `PR Gate` | `success` |
| Retained scope manifest | `ci-scope-bb0544c46d4b3fc3a7c96cb848e10cdecb4be040`, not expired |
| Scope manifest expiry | `2026-09-12T15:12:06Z` |

The run passed Portal, Core proof, coverage, package, Web, Creator, macOS,
dependency, version, and Assembly lanes. Its first Linux ASan attempt completed
76/77 tests and timed out in `host.native` with no sanitizer finding;
failed-only attempt 2 ran on `contabo-lmdj-linux` and passed the full 77/77
suite plus the 5/5 stress tier with no sanitizer finding. The rerun completed
with 19 successful formal jobs and no failure.

**The retained scope manifest expires `2026-09-12T15:12:06Z`.** If a release
transition has not consumed this evidence by then, rerun all jobs on the same
run ID and SHA to produce a fresh latest attempt. If that is unavailable, only
a newly authorized exact-SHA full run plus a separately reviewed intent update
may replace it. The evidence is never reconstructed, inferred, or backfilled.

## Preserved open boundaries

These rows are explicit and remain open. They are recorded here so that a
`canary` Release cannot be mistaken for their completion.

- [#360](https://github.com/endaye/lmdj/issues/360) Stage 9 physical and manual
  Sequence acceptance, and its five children
  [#362](https://github.com/endaye/lmdj/issues/362),
  [#363](https://github.com/endaye/lmdj/issues/363),
  [#364](https://github.com/endaye/lmdj/issues/364),
  [#365](https://github.com/endaye/lmdj/issues/365),
  [#366](https://github.com/endaye/lmdj/issues/366), are deferred and
  unverified. Their issue text still names the historical `1.0.37.0` candidate
  and must be retargeted to `1.0.40.0` before execution. #360 explicitly
  excludes tag, Release, deployment, publication, and Channel promotion from
  its own scope, so it does not gate this `canary` intent; it does gate any
  `beta` or `stable` claim.
- [#442](https://github.com/endaye/lmdj/issues/442) preserve Creator transport
  across a Sequence switch boundary under pressure, and
  [#443](https://github.com/endaye/lmdj/issues/443) intermittent creator-web
  redness, are open `p1` defects. Neither is fixed in `1.0.40.0`. Shipping this
  Build to `canary` ships them.

This intent follows the `1.0.36.0` precedent, which was published to `canary`
with open manual and physical verification rows preserved as explicit
boundaries rather than converted into automated Proof.

## What this record does not authorize

No Product tag, no Draft Release, no GitHub Release publication, no Runtime
deployment, and no Channel promotion. Each is a separate authorization and
verification boundary requiring its own audit before and after, and at most one
mutation per authorized turn.

## Publication record (2026-09-01)

`lmdj-v1.0.40.0` was published through the protected `publish-release.yml`
workflow, run
[33453258287](https://github.com/endaye/lmdj/actions/runs/33453258287),
after its `verify-draft` and pre-publication exact-tag remote audit both
passed. The immutable signed tag object
`c858b5b50b346861ed54542453b97ee7b1f7476e` peels to the authorized target
`bb0544c46d4b3fc3a7c96cb848e10cdecb4be040`.

| Field | Value |
| --- | --- |
| Release ID | `380126903` |
| Published at | `2026-09-01T00:05:52Z` |
| Release URL | <https://github.com/endaye/lmdj/releases/tag/lmdj-v1.0.40.0> |
| Draft / prerelease | `false` / `true` (canary policy) |
| Plan SHA-256 | `cfa50900c0b783d2811c997c5acf114880a4384e8e89ef8096efcd6e61ba039f` |

| Asset ID | Name | Bytes | SHA-256 |
| --- | --- | ---: | --- |
| `538674567` | `lmdj-web-runtime-host-2.1.1-product-1.0.40.0.zip` | `952095` | `e1581f487ac83a62ac9d497ef16a61c6410c0e16b4d6da42e2bd2c8242c452f4` |
| `538674585` | `lmdj-web-runtime-host-2.1.1-product-1.0.40.0.zip.sha256` | `115` | `9248ccd9386bfa354686292821291ec861947ffe333a3c9d0df294599b3e0b6a` |
| `538674588` | `lmdj-web-runtime-host-2.1.1-product-1.0.40.0.zip.sha256.asc` | `265` | `7860355597a274fd806dd9a9f46ce893a1ff860dfa653c7e5e4122934e89e204` |

The workflow's publish mutation succeeded, but its publish step then reported
`GitHub Release assets changed during publication` and skipped the in-run
post-publication audit. The three asset IDs, names, sizes, and SHA-256 digests
above remained exact; their GitHub `updated_at` timestamps also predate the
publication. A fresh independent `verify-draft` against Release ID `380126903`
downloaded and verified the published assets, checksum signature, profile, and
the same plan digest, and returned `already-published`. The failure therefore
records a post-publication asset metadata projection change, not an asset byte
or inventory change.

Runtime deployment and Channel promotion remain unperformed independent
boundaries.
